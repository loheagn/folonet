[package]
name = "folonet-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.12", optional = true }
network-types = "0.0.5"
byteorder = { version = "1", default-features = false }
bitflags = "2.4.1"

[lib]
path = "src/lib.rs"
